{{#if blogPost.comments}}
  <section>
    <h2>Comments</h2>
    <ul>
      {{#each blogPost.comments as |comment|}}
        <li>{{comment.commentText}} - {{comment.dateCreated}}</li>
      {{/each}}
    </ul>
  </section>
{{/if}}
