<nav class="navbar navbar-expand-md bg-dark py-3">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="/">
            <span class="bs-icon-sm bs-icon-rounded bs-icon-primary d-flex justify-content-center align-items-center me-2 bs-icon">
                <!-- SVG Logo here, as before -->
{{{get_emoji}}}
            </span>
            Dev Blog
        </a>
        <!-- Simplified navigation area -->
        <div class="ms-auto">
            {{#if loggedIn}}
                <form action="/api/signout" method="post" class="d-inline">
                    <button class="btn btn-light me-2" type="submit">Sign Out</button>
                </form>
                <a href="/blogs/new" class="btn btn-primary" role="button">Create a New Blog Post</a>
            {{else}}
                <a href="/signin" class="btn btn-light me-2" role="button">Sign In</a>
                <a href="/signup" class="btn btn-primary" role="button">Sign Up</a>
            {{/if}}
        </div>
    </div>
      <link rel="stylesheet" href="{{stylesheets}}">
</nav>

