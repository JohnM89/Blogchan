<header>
    <h1>Welcome to the Blog!</h1>
    <p>Sign in to make a blog post!</p>
    <nav>
        {{#if loggedIn}}
            <form action="/signout" method="post">
        <button type="submit">Sign Out</button>
        </form>
            <button><a href="/blogs/new">Create a New Blog Post</a></button>
        {{else}}
            <a href="/signin">Sign In</a>
            <a href="/signup">Sign Up</a>
        {{/if}}
    </nav>
</header>
<section>
    <div>
    {{#each posts}}
        {{> blogpost}}
        {{> comment}}
        <div class="vote-buttons">
  <form action="/upvote/{{id}}?_method=PUT" method="POST">
    <button type="submit">👍 {{upVotes}}</button> 
  </form>
  <form action="/downvote/{{id}}?_method=PUT" method="POST">
    <button type="submit">👎 {{downVotes}}</button>
  </form>
  
</div>
{{> deletepost id=this.id}}
{{> editpost id=this.id}}

        
    {{/each}}
       
</section>
<footer>
    <p>&copy; 2024 BlogChan</p>
